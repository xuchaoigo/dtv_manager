#xuc

COMPILE
- Need to link libs in dir ../lib. You could copy these libs from /home/xuc/dbus/lib.

RUNNING
- Run ./manager
- Log in on another terminal and run ./call.py to call the manager's method.

- MER environment is needed.


